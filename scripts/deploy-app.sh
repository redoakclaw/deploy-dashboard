#!/bin/bash
# Usage: deploy-app.sh <app-id>
# Reads app config from data/apps.json and runs its deploy script.
# This is a placeholder — Claude Code will build the real orchestration.

set -euo pipefail

echo "Deploy orchestration will be built by Claude Code"
echo "App ID: ${1:-<none>}"
exit 0